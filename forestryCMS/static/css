/* This is a CSS file, it doesn't contain any executable code. It contains styles that are used by the website. Here is a sample of what it might look like. */

body {
    background-color: #f0f0f0;
    font-family: Arial, sans-serif;
}

header {
    background-color: #333;
    color: #fff;
    padding: 10px;
    text-align: center;
}

.post-title {
    font-size: 24px;
    color: #333;
}

.post-content {
    font-size: 16px;
    color: #666;
}

.page-title {
    font-size: 20px;
    color: #333;
}

.page-content {
    font-size: 16px;
    color: #666;
}